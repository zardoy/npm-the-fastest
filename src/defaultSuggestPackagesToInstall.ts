export const defaultSuggestPackagesToInstall = {
    execa: {
        // package:
        // export: '' = key,
        // alias?: string
        // path: = './',
        type: 'named',
    },
    arrify: true,
}
